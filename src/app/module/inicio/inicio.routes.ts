import { Routes } from '@angular/router';
import { InicioIndexComponent } from './components/inicio-index/inicio-index.component';


export const routes: Routes = [
    { path: '', component: InicioIndexComponent},
    // { path: 'nuevo', component: ClientFromComponent },

];
